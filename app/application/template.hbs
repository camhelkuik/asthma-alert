<div class="container">
  
  <nav class="navbar navbar-inverse">
    <div class="container-fluid">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#main-navbar">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        {{#link-to "index" class="navbar-brand"}}Asthma Alert{{/link-to}}
      </div>

      <div class="collapse navbar-collapse" id="main-navbar">
        <ul class="nav navbar-nav">
          {{#link-to "index" tagName="li"}}<a href="">Home</a>{{/link-to}}
          {{#link-to "tracking" tagName="li"}}<a href="">Tracking</a>{{/link-to}}
          {{#link-to "daily-aq" tagName="li"}}<a href="">Daily Air Quality</a>{{/link-to}}
          {{#if session.isAuthenticated}}
            <a href=""{{action "invalidateSession"}}>Logout</a>
          {{else}}
            {{#link-to "login" tagName="li"}}<a href="">Login</a>{{/link-to}}
          {{/if}}
        </ul>
      </div>
    </div>
  </nav>

  {{outlet}}
</div>